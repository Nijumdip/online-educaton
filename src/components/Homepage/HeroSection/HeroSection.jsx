import { Button } from "../../../StylesComponent/Button";

const HeroSection = () => {
    return (
        <div>
            <Button>start a course</Button>
        </div>
    );
};

export default HeroSection;
